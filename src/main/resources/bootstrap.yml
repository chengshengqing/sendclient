eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: sendClient
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev